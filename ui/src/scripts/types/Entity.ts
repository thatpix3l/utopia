export type User = {
    id: number;
    firstName: string;
    lastName: string;
};

export type Contact = {
    id: number;
    firstName: string;
    lastName: string;
    phone: string;
    email: string;
};
